import { PatentFilterTypeToStringPipe } from './patent-filter-type-to-string.pipe';

describe('PatentFilterTypeToStringPipe', () => {
  it('create an instance', () => {
    const pipe = new PatentFilterTypeToStringPipe();
    expect(pipe).toBeTruthy();
  });
});
