import { DateArrayToStringPipe } from './date-array-to-string.pipe';

describe('DateArrayToStringPipe', () => {
  it('create an instance', () => {
    const pipe = new DateArrayToStringPipe();
    expect(pipe).toBeTruthy();
  });
});
